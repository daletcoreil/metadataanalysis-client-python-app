# metadataanalysis-client-python-app

## Requirements.

Python 3.4+

## Installation & Usage
### pip install

```sh
pip install -r requirements.txt
```

Edit 'app-config.json' configuration file with your Metadata Analysis API keys and media sample information.

Run the sample application:

```sh
python main.py
```
